# PROD FIX — rider dashboard, settle ও account (২৫ সেপ্টেম্বর ২০২৬)

**Reported:** *"Rider profile creation, dashboard kicui tik nai, account o open kora jacce na."*

**Root cause (প্রমাণসহ):** aaj-ker PR #33/#34 deploy holo (`proshanti.rahatahmed.site`),
kintu **production Supabase database-e notun 7 ta migration ekhono chalano hoy nai**
(`docs/VERIFICATION-2026-09-25.md`-eo likha ache je production migration state unverified).
Deployed code notun DB object call kore — object gulo database-e nei, tai rider
app-er protita read 503/error dey. PGlite-e duita schema pashapashi test kore
pramit kora hoyeche:

| App call (deployed code) | Prod DB-e (migration chalani) | Migration chalale |
|---|---|---|
| `GET /api/rider/jobs` → `ps_expire_stale_offers(p_force)` | `function … does not exist` → **feed 503** | OK |
| `GET /api/rider/settlements` → `rider_settle_claims` | `relation … does not exist` → **503** | OK |
| Deliver → `ps_rider_deliver_check` | `function … does not exist` → **PIN flow dead** | OK |
| Settle → `ps_rider_settle` | **purano function cholche — rider-er cash approval chhara-i zero hoy!** | claim file hoy, cash safe |
| Shop Ready → area broadcast | purano auto-offer (single rider logic) | area-er shob eligible rider pai |

Dashboard `Promise.all([jobs, settlements])` chalay — dui path-i fail, tai
**rider dashboard-er kichui dekhay na**. Ar notun rider-er account ta admin
approval + login link chhara khular nei — seta by-design, kintu UI-te ghapla
chhilo (niche fix kora hoyeche).

## ২ মিনিটের স্থায়ী সমাধান — SQL Editor-e krome 7ta file chalaben

### ⚠️ 25 tarikh-e chalanor somoy `ERROR: 42P01: relation "v_order" does not exist` ashle

Eta migration-er dos na — **SQL Editor boro paste kete fele**. Kata ongsho
function body-r bhitore porle plpgsql variable (`v_order`) ke table bhebe
execute kore. 2026-09-16-te o exact same ghotona hoyechilo (`relation "v_zone"
does not exist`) — tokhon thekei `supabase/paste-parts/` convention chole ashe.

**Somadhan:** `supabase/paste-parts/20260925-fixes/` folderer **23 ta chhoto
part** aoorder chiro, 01 theke 23, prottekta puro file copy-paste + RUN.
Base64 chunk parts puro copy hote hobe (assembler length/md5 check chara
run-i korbe na). Shob part repeat-safe — koto dur gechhe nishchit na thakle
01 theke abar shuru korun. Bisesh: oi folderer `README.md`.

PGlite-e purano (prod-er state) schema-r upor ei 23 part apply kore verify
kora hoyechhe: RUN 1 23/23 OK, abar RUN 2 (repeat-safety) 23/23 OK, tarpor
`ps_checkout_health` → `version 202609250007`, `broadcast_resume_ok /
settle_claims_ok / pin_lockout_ok / realtime_offers_ok` shob `true`.

### Boro paste-e kaj korle (alternative)

Supabase Dashboard → SQL Editor → prottek file paste + RUN, **ei order-e**
(shob file `supabase/migrations/`-e ache, repeate-safe):

1. `supabase/migrations/202609170001_two_tap_order_flow.sql` — age bad porle
2. `supabase/migrations/202609250001_area_broadcast_dispatch.sql`
3. `supabase/migrations/202609250002_dispatch_cancel_guard.sql`
4. `supabase/migrations/202609250003_dispatch_withdraw_resume.sql`
5. `supabase/migrations/202609250004_settle_claims.sql`
6. `supabase/migrations/202609250005_delivery_pin_lockout.sql`
7. `supabase/migrations/202609250006_dispatch_health.sql`
8. `supabase/migrations/202609250007_realtime_offers.sql`

### Verify (drei medium-ei)

SQL Editor-e `supabase/diagnose.sql` chalaben — row **37–37i shob
`present = true`** hote hobe. Tarpor `/api/health` (staff logged-in browser-e)
`broadcastResume`, `settleClaims`, `pinLockout`, `realtimeOffers` — shob `true`.

Ei migration gulo chalalei rider dashboard, offer/accept/pickup/deliver,
settle-claim, area dispatch — shob live hoye jabe. Niche-r code fixes emnitei
add kora hayechhe jate **migration chalanor age-o** app bhange na, rider-er
taka jhuve na, ar bhul message na dekhay.

## Ei branch-e kora code fix (migration chara-o app sakto thake)

| # | Fix | File |
|---|-----|------|
| 1 | Job feed ekhon migration-bakra DB-eo kaj kore — sweep fail hole list cholche (warn log-e migration-er nam lekha) | `src/lib/db/riders.ts` (`expireStaleAssignments`) |
| 2 | Settlements panel fail hole **job feed ar blank hoy na** (allSettled); `claimsReady:false` response-e ashe | `src/lib/use-rider.ts`, `api/rider/settlements` |
| 3 | **Taka-nerapotti guard:** claims table na thakle Settle tap 503 dey — purano `ps_rider_settle` ar rider-er cash zero korte pare na | `api/rider/settle`, `settleClaimsReady()` probe; UI-te button hide + notice |
| 4 | Deliver-er fallback ekhon sathe 42883-o dhore (age sudhu PGRST202) — pre-lockout DB-te PIN delivery kaj kore | `src/lib/db/riders.ts` |
| 5 | Raw SQL error ("function ps_… does not exist") rider-er phon-e jay na — Bangla 503 message jay | `api/rider/_lib.ts` |
| 6 | Admin riders queue claims-table missing-e crash korto → ekhon khali queue | `listPendingSettleClaims` |
| 7 | Login: unconfigured Supabase-e guest-loop redirect band; raw English auth error-er Bangla orth ("Email not confirmed" → confirm korte bolun); **signup sesh holei session thakle sorasori /rider/apply-e niye jay**, na hole email-confirmation clear instruction | `src/app/rider/login/page.tsx` |
| 8 | Apply form: zone select na korle ekhon submit-er **agei** Bangla error; success screen-e jodi login account linked na hoy (linked:false) → **"ei email diye account khulun" CTA** (email prefilled, signup tab khola) — ei gap-i chhilo "account open kora jacce na"-r karon; bhul OTP copy-o thik kora | `src/app/rider/apply/page.tsx` |

**Rider onboarding-er shudhu flow (bhasha bhangabe na):** `/rider/login` →
"নতুন অ্যাকাউন্ট" (email + password) → `/rider/apply` (form + zone) → admin
approve + link korte pare (na linked thakle email diye link kore) → login →
dashboard live.

Tests: `rider-degrade` (9 notun), `rider-route-degrade` (2), `settle-claim-routes`
+1 — tsc 0, eslint 0, 1,286+ tests pass, production build pass.
