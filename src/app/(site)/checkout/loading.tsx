import StorefrontSkeleton from "@/components/ui/storefront-skeleton";

/** Brand-shaped placeholder while the route streams — no blank flash. */
export default function Loading() {
  return <StorefrontSkeleton />;
}
